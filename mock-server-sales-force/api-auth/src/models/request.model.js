export default class {
  constructor(country, app, username, password, role) {
    this.country = country
    this.app = app
    this.username = username
    this.password = password
    this.role = role
  }
}
